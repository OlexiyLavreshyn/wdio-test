version: "3"

services:
  selenium:
    image: selenium/standalone-chrome
    ports:
      - "4444:4444"

  tests:
    build: .
    depends_on:
      - selenium
    environment:
      - SELENIUM_HOST=selenium